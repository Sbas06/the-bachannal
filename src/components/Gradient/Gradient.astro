---
import '@/components/Gradient/gradient.css';
const {
  containerClass,
  as: Component = 'div',
  isAnimated,
  hasGradient,
} = Astro.props;
---

<Component
  class={`relative ${containerClass} ${hasGradient ? (isAnimated ? 'animate-gradient' : 'gradient') : ''}`}
>
  <slot />
</Component>
